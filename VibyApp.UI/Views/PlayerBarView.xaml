<UserControl x:Class="VibyApp.UI.Views.PlayerBarView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" 
             d:DesignWidth="1100" Height="90">

    <UserControl.Resources>
        <Style x:Key="PlayerControlButton" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Foreground" Value="#94A3B8"/>
            <Setter Property="FontSize" Value="18"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Margin" Value="10,0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Foreground" Value="White"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="PlayPauseButton" TargetType="Button" BasedOn="{StaticResource PlayerControlButton}">
            <Setter Property="Foreground" Value="#07090F"/>
            <Setter Property="Background" Value="White"/>
            <Setter Property="Width" Value="38"/>
            <Setter Property="Height" Value="38"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="19">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#E2E8F0"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </UserControl.Resources>

    <Border Background="#07090F" BorderBrush="#1F2937" BorderThickness="0,1,0,0" Padding="20,10">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="300"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="300"/>
            </Grid.ColumnDefinitions>

            <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center">
                <Border Width="55" Height="55" Background="#1F2937" CornerRadius="6">
                    <TextBlock Text="🎵" HorizontalAlignment="Center" VerticalAlignment="Center" FontSize="24" Opacity="0.5"/>
                </Border>
                <StackPanel VerticalAlignment="Center" Margin="15,0">
                    <TextBlock Text="{Binding CurrentTrackName}" Foreground="White" FontSize="14" FontWeight="Bold" TextTrimming="CharacterEllipsis"/>
                    <TextBlock Text="{Binding CurrentArtistName}" Foreground="#64748B" FontSize="12" TextTrimming="CharacterEllipsis"/>
                </StackPanel>
                <Button Content="❤️" Style="{StaticResource PlayerControlButton}" FontSize="14" Margin="15,0,0,0"/>
            </StackPanel>

            <StackPanel Grid.Column="1" VerticalAlignment="Center">
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,0,0,5">
                    <Button Content="🔀" Style="{StaticResource PlayerControlButton}" Command="{Binding ToggleShuffleCommand}"/>
                    <Button Content="⏮" Style="{StaticResource PlayerControlButton}" Command="{Binding PreviousTrackCommand}"/>

                    <Button Command="{Binding TogglePlayPauseCommand}">
                        <Button.Style>
                            <Style TargetType="Button" BasedOn="{StaticResource PlayPauseButton}">
                                <Setter Property="Content" Value="▶"/>
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding IsPlaying}" Value="True">
                                        <Setter Property="Content" Value="⏸"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </Button.Style>
                    </Button>

                    <Button Content="⏭" Style="{StaticResource PlayerControlButton}" Command="{Binding NextTrackCommand}"/>
                    <Button Content="🔁" Style="{StaticResource PlayerControlButton}" Command="{Binding ToggleRepeatCommand}"/>
                </StackPanel>

                <Grid Width="500" HorizontalAlignment="Center">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock Text="0:00" Foreground="#64748B" FontSize="11" VerticalAlignment="Center"/>
                    <Slider Grid.Column="1" Margin="10,0" VerticalAlignment="Center"
                            Maximum="{Binding TotalDuration}" 
                            Value="{Binding TimeCurrentPosition, Mode=TwoWay}"
                            Minimum="0"/>
                    <TextBlock Grid.Column="2" Text="3:45" Foreground="#64748B" FontSize="11" VerticalAlignment="Center"/>
                </Grid>
            </StackPanel>

            <StackPanel Grid.Column="2" Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center">
                <Button Content="📋" Style="{StaticResource PlayerControlButton}" FontSize="14"/>
                <Button Content="🔊" Style="{StaticResource PlayerControlButton}" FontSize="14"/>
                <Slider Width="100" VerticalAlignment="Center" Margin="5,0,0,0" Value="{Binding Volume, Mode=TwoWay}" Maximum="100"/>
            </StackPanel>
        </Grid>
    </Border>
</UserControl>