<UserControl x:Class="VibyApp.UI.Views.HomeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="720" d:DesignWidth="1100"
             Background="#0F111A">

    <UserControl.Resources>
        <Style x:Key="AlbumCard" TargetType="Border">
            <Setter Property="CornerRadius" Value="12"/>
            <Setter Property="Background" Value="#14294a"/>
            <Setter Property="Margin" Value="8"/>
            <Setter Property="Cursor" Value="Hand"/>
        </Style>

        <Style x:Key="ArtistCard" TargetType="Border">
            <Setter Property="Background" Value="#335182"/>
            <Setter Property="Margin" Value="5"/>
            <Setter Property="Cursor" Value="Hand"/>
        </Style>
    </UserControl.Resources>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="230"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Border Background="#161922" BorderBrush="#222" BorderThickness="0,0,1,0">
            <DockPanel Margin="15,20">
                <StackPanel DockPanel.Dock="Top">
                    <TextBlock Text="VIBY" Foreground="White" FontSize="26" FontWeight="ExtraBold" Margin="10,0,10,40"/>
                    <Button Content="🏠 Accueil" Style="{StaticResource SidebarButtonStyle}" Foreground="White" Background="#1F2937"/>
                    <Button Content="🔍 Explorer" Style="{StaticResource SidebarButtonStyle}"/>
                    <Button Content="📚 Bibliothèque" Style="{StaticResource SidebarButtonStyle}"/>
                    <Button Content="👤 Profil" Style="{StaticResource SidebarButtonStyle}" Click="Profile_Click"/>
                </StackPanel>

                <StackPanel DockPanel.Dock="Bottom" VerticalAlignment="Bottom">
                    <TextBlock Text="VOS MUSIQUES FAVORITES" Foreground="#4B5563" FontSize="11" FontWeight="Bold" Margin="15,0,0,10"/>
                    <Button Content="❤️ Titres likés" Style="{StaticResource SidebarButtonStyle}"/>
                    <Button Content="📻 Albums" Style="{StaticResource SidebarButtonStyle}"/>
                    <Button Content="👤 Artistes" Style="{StaticResource SidebarButtonStyle}"/>
                </StackPanel>
                <Grid />
            </DockPanel>
        </Border>

        <Grid Grid.Column="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="80"/>
                <RowDefinition Height="1.5*"/>
                <RowDefinition Height="1.2*"/>
                <RowDefinition Height="1*"/>
            </Grid.RowDefinitions>

            <Border Width="400" Height="40" Background="#1A1D29" CornerRadius="20" 
                    HorizontalAlignment="Left" Margin="40,0" VerticalAlignment="Center">
                <Grid Margin="15,0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock Text="🔍" Foreground="#6B7280" VerticalAlignment="Center" Margin="0,0,10,0"/>
                    <TextBox Grid.Column="1" Background="Transparent" BorderThickness="0" Foreground="White" 
                             VerticalAlignment="Center" CaretBrush="White" Text="Recherche"/>
                </Grid>
            </Border>

            <Grid Grid.Row="1" Margin="40,0">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                <TextBlock Text="Hits du Moment" Foreground="White" FontSize="22" FontWeight="Bold" Margin="0,0,0,10"/>

                <UniformGrid Grid.Row="1" Columns="5">
                    <Border Style="{StaticResource AlbumCard}">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="*"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>
                            <Border CornerRadius="8" Margin="10">
                                <Border.Background>
                                    <ImageBrush ImageSource="https://e-cdns-images.dzcdn.net/images/cover/e2408376993181816e8739665d918663/500x500-000000-80-0-0.jpg" Stretch="UniformToFill"/> <!-- changer l'url pour la bonne image -->
                                </Border.Background>
                            </Border>
                            <StackPanel Grid.Row="1" Margin="10,0,10,10">
                                <TextBlock Text="Stay" Foreground="White" FontWeight="Bold" FontSize="14" TextTrimming="CharacterEllipsis"/> <!-- changer le nom pour le bon titre -->
                                <TextBlock Text="The Kid LAROI" Foreground="#9CA3AF" FontSize="11"/> <!-- changer le nom pour le bon artiste -->
                            </StackPanel>
                        </Grid>
                    </Border>
                    <Border Style="{StaticResource AlbumCard}" Opacity="0.5"/>
                    <Border Style="{StaticResource AlbumCard}" Opacity="0.5"/>
                    <Border Style="{StaticResource AlbumCard}" Opacity="0.5"/>
                    <Border Style="{StaticResource AlbumCard}" Opacity="0.5"/>
                </UniformGrid>
            </Grid>

            <Grid Grid.Row="2" Margin="40,10">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                <TextBlock Text="Artistes Populaires" Foreground="White" FontSize="20" FontWeight="Bold" Margin="0,0,0,5"/>

                <UniformGrid Grid.Row="1" Columns="5">
                    <Border Style="{StaticResource ArtistCard}">
                        <StackPanel VerticalAlignment="Center">
                            <Ellipse Width="90" Height="90" HorizontalAlignment="Center">
                                <Ellipse.Fill>
                                    <ImageBrush ImageSource="https://api.deezer.com/artist/27/image" Stretch="UniformToFill"/> <!-- changer l'url pour la bonne image -->
                                </Ellipse.Fill>
                            </Ellipse>
                            <TextBlock Text="Dua Lipa" Foreground="White" HorizontalAlignment="Center" Margin="0,8,0,0" FontSize="13"/> <!-- changer le nom pour le bon artiste -->
                        </StackPanel>
                    </Border>
                    <Border Style="{StaticResource ArtistCard}" Opacity="0.5"/>
                    <Border Style="{StaticResource ArtistCard}" Opacity="0.5"/>
                    <Border Style="{StaticResource ArtistCard}" Opacity="0.5"/>
                    <Border Style="{StaticResource ArtistCard}" Opacity="0.5"/>
                </UniformGrid>
            </Grid>

            <Grid Grid.Row="3" Margin="40,10,40,20">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                <TextBlock Text="Genres à la Mode" Foreground="White" FontSize="20" FontWeight="Bold" Margin="0,0,0,10"/>

                <UniformGrid Grid.Row="1" Columns="4">
                    <Border Background="#EF4444" CornerRadius="12" Margin="5">
                        <TextBlock Text="🔥 Pop" Foreground="White" VerticalAlignment="Center" HorizontalAlignment="Center" FontWeight="Bold"/>
                    </Border>
                    <Border Background="#F59E0B" CornerRadius="12" Margin="5">
                        <TextBlock Text="🎧 Hip Hop" Foreground="White" VerticalAlignment="Center" HorizontalAlignment="Center" FontWeight="Bold"/>
                    </Border>
                    <Border Background="#3B82F6" CornerRadius="12" Margin="5">
                        <TextBlock Text="⚡ Electro" Foreground="White" VerticalAlignment="Center" HorizontalAlignment="Center" FontWeight="Bold"/>
                    </Border>
                    <Border Background="#10B981" CornerRadius="12" Margin="5">
                        <TextBlock Text="🎸 Rock" Foreground="White" VerticalAlignment="Center" HorizontalAlignment="Center" FontWeight="Bold"/>
                    </Border>
                </UniformGrid>
            </Grid>
        </Grid>
    </Grid>
</UserControl>