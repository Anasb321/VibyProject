<UserControl x:Class="VibyApp.UI.Views.HomeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="720" d:DesignWidth="1100"
             Background="#0F111A">

    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis" />

        <Style x:Key="AlbumCard" TargetType="Border">
            <Setter Property="CornerRadius" Value="12" />
            <Setter Property="Background" Value="#14294a" />
            <Setter Property="Margin" Value="8" />
            <Setter Property="Cursor" Value="Hand" />
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#1c3a69" />
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="ArtistCard" TargetType="Border">
            <Setter Property="Background" Value="#335182" />
            <Setter Property="Margin" Value="5" />
            <Setter Property="CornerRadius" Value="12" />
            <Setter Property="Cursor" Value="Hand" />
        </Style>

        <Style x:Key="SidebarButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="Foreground" Value="#9CA3AF" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Padding" Value="15,10" />
            <Setter Property="HorizontalContentAlignment" Value="Left" />
            <Setter Property="FontSize" Value="14" />
        </Style>
    </UserControl.Resources>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="230" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <Border Background="#161922" BorderBrush="#222" BorderThickness="0,0,1,0">
            <DockPanel Margin="15,20">
                <StackPanel DockPanel.Dock="Top">
                    <TextBlock Text="VIBY" Foreground="White" FontSize="26" FontWeight="ExtraBold" Margin="10,0,10,40" />
                    <Button Content="🏠 Accueil" Style="{StaticResource SidebarButtonStyle}" Foreground="White"
                            Background="#1F2937" />
                    <Button Content="🔍 Explorer" Style="{StaticResource SidebarButtonStyle}" />
                    <Button Content="📚 Bibliothèque" Style="{StaticResource SidebarButtonStyle}" />
                    <Button Content="👤 Profil" Style="{StaticResource SidebarButtonStyle}" />
                </StackPanel>
                <StackPanel DockPanel.Dock="Bottom" VerticalAlignment="Bottom">
                    <TextBlock Text="VOS MUSIQUES FAVORITES" Foreground="#4B5563" FontSize="11" FontWeight="Bold"
                               Margin="15,0,0,10" />
                    <Button Content="❤️ Favoris" Style="{StaticResource SidebarButtonStyle}" />
                    <Button Content="📻 Albums" Style="{StaticResource SidebarButtonStyle}" />
                    <Button Content="👤 Artistes" Style="{StaticResource SidebarButtonStyle}" />
                </StackPanel>
            </DockPanel>
        </Border>

        <Grid Grid.Column="1">
    <Grid.RowDefinitions>
        <RowDefinition Height="80" />
        <RowDefinition Height="*" /> </Grid.RowDefinitions>

    <Border Width="400" Height="40" Background="#1A1D29" CornerRadius="20" HorizontalAlignment="Left" Margin="40,0" VerticalAlignment="Center">
        <Grid Margin="15,0">
            <Grid.ColumnDefinitions><ColumnDefinition Width="Auto" /><ColumnDefinition Width="*" /></Grid.ColumnDefinitions>
            <TextBlock Text="🔍" Foreground="#6B7280" VerticalAlignment="Center" Margin="0,0,10,0" />
            <TextBox Grid.Column="1" Background="Transparent" BorderThickness="0" Foreground="White" VerticalAlignment="Center" CaretBrush="White" Opacity="0.5" Text="Recherche..." />
        </Grid>
    </Border>

    <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" Padding="0,0,0,20">
        <StackPanel Margin="40,0">
            
            <TextBlock Text="Top 10 des sons les plus écouté du moment" Foreground="White" FontSize="24" FontWeight="Bold" Margin="0,20,0,15" />
            
            <ItemsControl ItemsSource="{Binding TopTracks}">
                <ItemsControl.Style>
                    <Style TargetType="ItemsControl">
                        <Setter Property="Visibility" Value="Collapsed" />
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding IsLoading}" Value="False"><Setter Property="Visibility" Value="Visible" /></DataTrigger>
                        </Style.Triggers>
                    </Style>
                </ItemsControl.Style>
                <ItemsControl.ItemsPanel><ItemsPanelTemplate><UniformGrid Columns="5" /></ItemsPanelTemplate></ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border Style="{StaticResource AlbumCard}" ClipToBounds="True">
                            <Grid Height="260"> <Image Source="{Binding Album.CoverUrl}" Stretch="UniformToFill" Opacity="0.4">
                                    <Image.Effect><BlurEffect Radius="40" /></Image.Effect>
                                </Image>
                                <Border Background="#66000000" />
                                <Grid>
                                    <Grid.RowDefinitions><RowDefinition Height="*" /><RowDefinition Height="Auto" /></Grid.RowDefinitions>
                                    <Border CornerRadius="8" Margin="12" BorderBrush="#33FFFFFF" BorderThickness="1">
                                        <Border.Background><ImageBrush ImageSource="{Binding Album.CoverUrl}" Stretch="UniformToFill" /></Border.Background>
                                    </Border>
                                    <StackPanel Grid.Row="1" Margin="12,0,12,15">
                                        <TextBlock Text="{Binding Title}" Foreground="White" FontWeight="Bold" FontSize="15" TextTrimming="CharacterEllipsis">
                                            <TextBlock.Effect><DropShadowEffect BlurRadius="2" ShadowDepth="1" Opacity="0.8"/></TextBlock.Effect>
                                        </TextBlock>
                                        <TextBlock Text="{Binding Artist.Name}" Foreground="#B3B3B3" FontSize="12" FontWeight="Medium"/>
                                    </StackPanel>
                                </Grid>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>

            <TextBlock Text="Top 10 des artistes les plus populaires" Foreground="White" FontSize="22" FontWeight="Bold" Margin="0,40,0,15" />
            
            <ItemsControl ItemsSource="{Binding TopArtists}">
                <ItemsControl.ItemsPanel><ItemsPanelTemplate><UniformGrid Columns="5" /></ItemsPanelTemplate></ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border Style="{StaticResource ArtistCard}" ClipToBounds="True">
                            <Grid Height="180">
                                <Image Source="{Binding PictureUrl}" Stretch="UniformToFill" Opacity="0.4">
                                    <Image.Effect><BlurEffect Radius="30" /></Image.Effect>
                                </Image>
                                <Border Background="#55000000" />
                                <StackPanel VerticalAlignment="Center">
                                    <Ellipse Width="95" Height="95" HorizontalAlignment="Center">
                                        <Ellipse.Fill><ImageBrush ImageSource="{Binding PictureUrl}" Stretch="UniformToFill" /></Ellipse.Fill>
                                    </Ellipse>
                                    <TextBlock Text="{Binding Name}" Foreground="White" FontWeight="Bold" FontSize="15" HorizontalAlignment="Center" Margin="10,10,10,0">
                                        <TextBlock.Effect><DropShadowEffect BlurRadius="2" ShadowDepth="1" /></TextBlock.Effect>
                                    </TextBlock>
                                </StackPanel>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>

            <TextBlock Text="Genres à la Mode" Foreground="White" FontSize="22" FontWeight="Bold" Margin="0,40,0,15" />
            <UniformGrid Columns="4" Margin="0,0,0,20">
                <Border Background="#EF4444" Height="80" CornerRadius="12" Margin="5"><TextBlock Text="🔥 Pop" Foreground="White" VerticalAlignment="Center" HorizontalAlignment="Center" FontWeight="Bold" FontSize="16"/></Border>
                <Border Background="#F59E0B" Height="80" CornerRadius="12" Margin="5"><TextBlock Text="🎧 Hip Hop" Foreground="White" VerticalAlignment="Center" HorizontalAlignment="Center" FontWeight="Bold" FontSize="16"/></Border>
                <Border Background="#3B82F6" Height="80" CornerRadius="12" Margin="5"><TextBlock Text="⚡ Electro" Foreground="White" VerticalAlignment="Center" HorizontalAlignment="Center" FontWeight="Bold" FontSize="16"/></Border>
                <Border Background="#10B981" Height="80" CornerRadius="12" Margin="5"><TextBlock Text="🎸 Rock" Foreground="White" VerticalAlignment="Center" HorizontalAlignment="Center" FontWeight="Bold" FontSize="16"/></Border>
            </UniformGrid>
        </StackPanel>
    </ScrollViewer>
</Grid>
    </Grid>
</UserControl>