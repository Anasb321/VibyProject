<UserControl x:Class="VibyApp.UI.Views.ProfileView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="900"
             Background="#0F111A">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="230"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Border Background="#161922" BorderBrush="#222" BorderThickness="0,0,1,0">
            <DockPanel Margin="15,20">
                <StackPanel DockPanel.Dock="Top">
                    <TextBlock Text="VIBY" Foreground="White" FontSize="26" FontWeight="ExtraBold" Margin="10,0,10,40"/>
                    <Button Content="🏠 Accueil" Style="{StaticResource SidebarButtonStyle}" Click="Home_Click"/>
                    <Button Content="🔍 Explorer" Style="{StaticResource SidebarButtonStyle}"/>
                    <Button Content="📚 Bibliothèque" Style="{StaticResource SidebarButtonStyle}"/>
                    <Button Content="👤 Profil" Style="{StaticResource SidebarButtonStyle}" Foreground="White" Background="#1F2937"/>
                </StackPanel>

                <StackPanel DockPanel.Dock="Bottom" VerticalAlignment="Bottom">
                    <TextBlock Text="VOS MUSIQUES FAVORITES" Foreground="#4B5563" FontSize="11" FontWeight="Bold" Margin="15,0,0,10"/>
                    <Button Content="❤️ Titres likés" Style="{StaticResource SidebarButtonStyle}"/>
                    <Button Content="📻 Albums" Style="{StaticResource SidebarButtonStyle}"/>
                    <Button Content="👤 Artistes" Style="{StaticResource SidebarButtonStyle}"/>
                </StackPanel>
                <Grid />
            </DockPanel>
        </Border>

        <ScrollViewer Grid.Column="1" VerticalScrollBarVisibility="Auto">
            <StackPanel Margin="40">

                <StackPanel Orientation="Horizontal" Margin="0,0,0,30">
                    <Border Width="100" Height="100" CornerRadius="50">
                        <Border.Background>
                            <ImageBrush ImageSource="https://api.dicebear.com/7.x/avataaars/png?seed=Alex" Stretch="UniformToFill"/>
                        </Border.Background>
                    </Border>
                    <StackPanel Margin="25,0" VerticalAlignment="Center">
                        <TextBlock Text="{Binding Username}" Foreground="White" FontSize="28" FontWeight="Bold"/>
                        <TextBlock Text="{Binding UserEmail}" Foreground="#9CA3AF" FontSize="16"/>
                        <Border Background="#FFD700" HorizontalAlignment="Left" CornerRadius="4" Padding="8,2" Margin="0,8,0,0">
                            <TextBlock Text="MEMBRE PREMIUM" Foreground="Black" FontSize="10" FontWeight="Bold"/>
                        </Border>
                    </StackPanel>
                </StackPanel>

                <Separator Background="#222" Margin="0,10,0,30"/>

                <TextBlock Text="Informations personnelles" Foreground="White" FontSize="18" FontWeight="Bold" Margin="0,0,0,20"/>

                <UniformGrid Columns="2" Margin="0,0,0,30">
                    <StackPanel Margin="0,10">
                        <TextBlock Text="Membre depuis" Foreground="#4B5563" FontSize="12"/>
                        <TextBlock Text="Octobre 2019" Foreground="White" FontSize="15" Margin="0,5"/>
                    </StackPanel>
                    <StackPanel Margin="0,10">
                        <TextBlock Text="Pays" Foreground="#4B5563" FontSize="12"/>
                        <TextBlock Text="France" Foreground="White" FontSize="15" Margin="0,5"/>
                    </StackPanel>
                </UniformGrid>

                <TextBlock Text="Préférences de l'application" Foreground="White" FontSize="18" FontWeight="Bold" Margin="0,20,0,20"/>

                <Border Background="#161922" CornerRadius="12" Padding="20">
                    <StackPanel>
                        <DockPanel Margin="0,10">
                            <TextBlock Text="Thème Sombre" Foreground="White" VerticalAlignment="Center"/>
                            <CheckBox IsChecked="True" HorizontalAlignment="Right" DockPanel.Dock="Right"/>
                        </DockPanel>
                        <Separator Background="#222" Margin="0,15"/>
                        <DockPanel Margin="0,10">
                            <TextBlock Text="Langue" Foreground="White" VerticalAlignment="Center"/>
                            <TextBlock Text="Français" Foreground="#9CA3AF" HorizontalAlignment="Right" DockPanel.Dock="Right"/>
                        </DockPanel>
                    </StackPanel>
                </Border>

                <StackPanel Orientation="Horizontal" Margin="0,40,0,0">
                    <Button Content="Modifier le profil" Height="40" Width="160" Background="#1DB954" Foreground="White" BorderThickness="0" FontWeight="Bold">
                        <Button.Resources>
                            <Style TargetType="Border">
                                <Setter Property="CornerRadius" Value="20"/>
                            </Style>
                        </Button.Resources>
                    </Button>
                    <Button Content="Déconnexion" Height="40" Width="160" Margin="15,0" Background="#E74C3C" Foreground="White" BorderThickness="0" FontWeight="Bold">
                        <Button.Resources>
                            <Style TargetType="Border">
                                <Setter Property="CornerRadius" Value="20"/>
                            </Style>
                        </Button.Resources>
                    </Button>
                </StackPanel>

            </StackPanel>
        </ScrollViewer>
    </Grid>
</UserControl>