<Window x:Class="Top10Trend.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Top 10 trend artist and music" Height="600" Width="800"
        Background="#121212" Foreground="White">
    
    <Grid Margin="20">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <StackPanel Grid.Column="0" Margin="0,0,10,0">
            <TextBlock Text="Top 10 Titres du Moment" FontSize="20" FontWeight="Bold" Margin="0,0,0,15"/>
            <ListBox ItemsSource="{Binding TopTracks}" Background="Transparent" BorderThickness="0" ScrollViewer.HorizontalScrollBarVisibility="Disabled">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <StackPanel Orientation="Horizontal" Margin="0,5">
                            <Image Source="{Binding Album.CoverUrl}" Width="50" Height="50" Stretch="UniformToFill" Margin="0,0,10,0"/>
                            <StackPanel VerticalAlignment="Center">
                                <TextBlock Text="{Binding Title}" FontWeight="Bold" FontSize="14" Foreground="White"/>
                                <TextBlock Text="{Binding Artist.Name}" Foreground="#B3B3B3" FontSize="12"/>
                            </StackPanel>
                        </StackPanel>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
        </StackPanel>
        
        <Button Content="Refresh" 
                Click="RefreshButton" 
                Grid.ColumnSpan="2"
                HorizontalAlignment="Center"
                VerticalAlignment="Top"
                Width="100" Height="30" 
                Margin="10"
                Background="#2D2D2D" 
                Foreground="White" 
                BorderBrush="#555555"/>

        <StackPanel Grid.Column="1" Margin="10,0,0,0">
            <TextBlock Text="Top 10 Artistes" FontSize="20" FontWeight="Bold" Margin="0,0,0,15"/>
            <ListBox ItemsSource="{Binding TopArtists}" Background="Transparent" BorderThickness="0" ScrollViewer.HorizontalScrollBarVisibility="Disabled">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <StackPanel Orientation="Horizontal" Margin="0,5">
                            <Ellipse Width="50" Height="50" Margin="0,0,10,0">
                                <Ellipse.Fill>
                                    <ImageBrush ImageSource="{Binding PictureUrl}" Stretch="UniformToFill"/>
                                </Ellipse.Fill>
                            </Ellipse>
                            <TextBlock Text="{Binding Name}" VerticalAlignment="Center" FontWeight="Bold" FontSize="14" Foreground="White"/>
                        </StackPanel>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
        </StackPanel>
    </Grid>
</Window>